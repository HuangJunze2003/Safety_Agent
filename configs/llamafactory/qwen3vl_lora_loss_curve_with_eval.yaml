### 与短程 loss 曲线同口径，但开启按步验证（会写出 training_eval_loss.png）

stage: sft
do_train: true
finetuning_type: lora
lora_target: q_proj,k_proj,v_proj,o_proj,gate_proj,up_proj,down_proj
lora_rank: 64
lora_alpha: 128
lora_dropout: 0.05

model_name_or_path: /root/autodl-tmp/Safety_Agent/models/base-vl
template: qwen2_vl

dataset_dir: /root/autodl-tmp/Safety_Agent/data/data_processed
dataset: safety_qwen3vl_sft_cleaned_image
max_samples: 100000
cutoff_len: 2048
overwrite_cache: true
preprocessing_num_workers: 8

output_dir: /root/autodl-tmp/Safety_Agent/outputs/qwen3vl_lora_loss_curve_run
logging_steps: 1
save_steps: 1000
plot_loss: true
overwrite_output_dir: true

per_device_train_batch_size: 1
gradient_accumulation_steps: 8
learning_rate: 0.00005
num_train_epochs: 4.0
max_steps: 48
lr_scheduler_type: cosine
warmup_ratio: 0.03
bf16: true
gradient_checkpointing: true

val_size: 0.1
per_device_eval_batch_size: 1
eval_strategy: steps
eval_steps: 16

freeze_vision_tower: false
